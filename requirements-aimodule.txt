# AI Module — Applied ML in Production
# Python 3.12. Versions match the environment the notebooks were executed with.

# Core data stack
numpy==2.5.1
pandas==3.0.3
matplotlib==3.11.0
seaborn==0.13.2

# Modelling
scikit-learn==1.9.0
imbalanced-learn==0.14.2

# Experiment tracking (session 7 uses a sqlite backend, not the legacy file store)
mlflow==3.16.0

# Serving
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
streamlit==1.63.0
joblib==1.5.3

# Authoring and publication
jupyterlab  # any recent version; the stats environment runs notebooks through nbconvert
nbconvert==7.17.1
ipykernel==7.3.0
mkdocs==1.6.1
pymdown-extensions==11.0.1  # required by mkdocs.yml's pymdownx.* extensions

# Testing
pytest==9.1.1
